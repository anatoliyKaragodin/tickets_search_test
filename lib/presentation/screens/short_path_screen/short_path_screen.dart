import 'package:flutter/material.dart';

class ShortPathScreen extends StatelessWidget {
  const ShortPathScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Короче'));
  }
}