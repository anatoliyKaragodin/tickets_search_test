import 'package:flutter/material.dart';

class SubscribesScreen extends StatelessWidget {
  const SubscribesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Подписки'));
  }
}